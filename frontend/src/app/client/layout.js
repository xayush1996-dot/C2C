export const metadata = {
  title: "Client Workspace",
  description: "Your personalized coaching space. Access your calendar, worksheets, and resources.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ClientLayout({ children }) {
  return children;
}

export const metadata = {
  title: "Admin Dashboard",
  description: "Internal administrative console for Confusion to Clarity.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminLayout({ children }) {
  return children;
}
